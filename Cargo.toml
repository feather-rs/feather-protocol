[package]
name = "feather-protocol"
version = "0.1.0"
authors = ["caelunshun <caelunshun@gmail.com>"]
edition = "2018"

[dependencies]
bytes = "0.5"
smallbox = "0.8"
thiserror = "1.0"
anyhow = "1.0"

[build-dependencies]
feather-packet-parser = { path = "generator" }
async-std = { version = "1.1", features = ["attributes"] }

[workspace]
members = [".", "generator"]
